# Used only by the browser/web build. The Tauri desktop app always uses its
# local SQLite workspace and does not transmit data to Supabase by default.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
